using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestMapAgent : MonoBehaviour
{
    public Dictionary<QuestLine, QuestLineAgent> QuestMap
        = new Dictionary<QuestLine, QuestLineAgent>()
        {

        };
}
